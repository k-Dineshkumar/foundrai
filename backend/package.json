{
  "name": "foundrai-backend",
  "version": "1.0.1",
  "description": "FoundrAI REST API + SQLite database (zero native dependencies — uses Node's built-in node:sqlite)",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.7.3"
  }
}
